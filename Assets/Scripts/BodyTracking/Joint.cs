//using LightBuzz.Kinect4Azure;
//using LightBuzz;
//using System.Collections;
//using System.Collections.Generic;
//using UnityEngine;

namespace TrackManager.BodyTracking
{
    public class Joint
    {
        ////
        //// Summary:
        ////     The type of the joint.
        //public JointType JointType { get; internal set; }

        ////
        //// Summary:
        ////     The tracking state of the joint.
        //public TrackingState TrackingState { get; internal set; }

        ////
        //// Summary:
        ////     The position of the joint in the 3D world space.
        //public Vector3D Position { get; internal set; }

        ////
        //// Summary:
        ////     The orientation of the joint.
        //public Vector4D Orientation { get; internal set; }

        ////
        //// Summary:
        ////     The position of the joint in the 2D color space.
        //public Vector2D PositionColor { get; internal set; }

        ////
        //// Summary:
        ////     The position of the joint in the 2D depth space.
        //public Vector2D PositionDepth { get; internal set; }

        //// constructor for all the properties
        //public Joint(JointType jointType, TrackingState trackingState, Vector3D position, Vector4D orientation, Vector2D positionColor, Vector2D positionDepth)
        //{
        //    JointType = jointType;
        //    TrackingState = trackingState;
        //    Position = position;
        //    Orientation = orientation;
        //    PositionColor = positionColor;
        //    PositionDepth = positionDepth;
        //}
        //public Joint(Vector3D worldPos)
        //{
        //    Position = worldPos;
        //}

        //public Joint(JointType jointType, Vector3D worldPos)
        //{
        //    JointType = jointType;
        //    Position = worldPos;
        //}
    }
}